---
# Per-environment placement MOIDs, selected by the config-scoped label
# vspherevm.resources.stuttgart-things.com/environment (here: labda). The XR's
# spec.environmentConfig must match this value. MOIDs below are LabDA IDs.
apiVersion: apiextensions.crossplane.io/v1beta1
kind: EnvironmentConfig
metadata:
  name: vspherevm-labda
  labels:
    vspherevm.resources.stuttgart-things.com/environment: labda
data:
  templateUuid: 4221ffec-016e-5533-0435-1103fb1a611e
  datastoreId: datastore-12
  resourcePoolId: resgroup-8
  networkId: dvportgroup-2048
  folder: stuttgart-things/testing
  firmware: bios
  guestId: ubuntu64Guest
  scsiType: pvscsi
  adapterType: vmxnet3
  annotation: VSPHERE-VM BUILD w/ CROSSPLANE FOR STUTTGART-THINGS
  providerConfigName: default
