# Fixture copy of vault/app-secret-profile/examples/profiles/homerun2.yaml — the AppSecretProfile xr-rancher's profiles fetch (#464).
# Keep in step with the source; crossplane render observes nothing on its own.
apiVersion: secrets.stuttgart-things.com/v1alpha1
kind: AppSecretProfile
metadata:
  name: homerun2
spec:
  mount: homerun2
  entries:
    - suffix: ""
      keys:
        redisPassword:
          generate: {length: 32}
          overridable: true
        authToken:
          generate: {length: 32}
          overridable: true
        githubToken:
          shared: git-pat
