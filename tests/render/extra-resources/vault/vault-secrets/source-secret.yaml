# Fixture: examples/source-secret.yaml as the API server stores it. `crossplane
# render` does not fold stringData into data, so the example itself resolves to
# nothing offline.
apiVersion: v1
kind: Secret
metadata:
  name: demo-source
  namespace: default
type: Opaque
data:
  admin-token: RklYVFVSRS1BRE1JTi1UT0tFTg== # pragma: allowlist secret
