# Source Secret for xr.yaml / xr-max.yaml. MUST be in the XR's namespace —
# secretKeyRef has no namespace field on purpose (see the XRD).
apiVersion: v1
kind: Secret
metadata:
  name: demo-source
  namespace: default
type: Opaque
stringData:
  admin-token: EXAMPLE-ADMIN-TOKEN-REPLACE # pragma: allowlist secret
